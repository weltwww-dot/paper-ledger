# 论文台账 · 部署指南（让他人可访问）

> 当前网站是纯静态页面，数据存放在访问者浏览器（localStorage）。
> 要让课题组其他人访问，需要把网站部署到服务器；要共享/多人录入数据，需要后端。

---

## 现状与限制

| 数据 | 存放位置 | 谁可见 |
| --- | --- | --- |
| 内置论文（data/papers.js） | 网站文件，由 agent 更新 | 所有访问者（部署后） |
| 成员录入 / 导入的论文 | 各自浏览器 localStorage | 仅本人 |

因此两条路线：

- **路线 A · 静态托管（推荐先做）**：网站部署到静态托管平台，任何有链接的人都能访问。
  我作为维护者更新论文 → 发布 → 所有人看到最新内容。成员本地录入的数据仅本人可见。
- **路线 B · 加后端**：数据存到云端数据库，成员录入/导入的数据全组共享。
  适合需要多人协作录入的场景，成本和复杂度更高。

---

## 路线 A · 静态托管（零后端，最快可访问）

网站本身就是静态文件（index.html + css + js + 数据），可直接部署到任意静态托管。

### A1 · GitHub Pages（推荐，免费，可自动部署）

1. 在 GitHub 建一个仓库（如 `paper-ledger`），把本目录推上去：
   ```bash
   git init
   git add .
   git commit -m "论文台账初始版本"
   git branch -M main
   git remote add origin https://github.com/<你的账号>/paper-ledger.git
   git push -u origin main
   ```
2. 仓库 Settings → Pages → Source 选 `main` 分支 / root 目录 → Save
3. 访问地址：`https://<你的账号>.github.io/paper-ledger/`
4. 以后我每次「更新」：改完本地文件 → `git push` → GitHub Pages 自动更新

> 国内访问 GitHub Pages 可能不稳定；可选 Gitee Pages（需实名）或云厂商静态托管。

### A2 · 腾讯云 COS / 阿里云 OSS（国内访问快）

1. 开通对象存储（COS/OSS），创建一个公开读的存储桶
2. 把本目录全部文件上传到桶根目录
3. 开启静态网站托管，得到访问地址；可绑定自定义域名 + CDN
4. 以后更新：重新上传 `data/papers.js`（以及新增的 summaries 不强制，PDF 按需）

### A3 · Vercel / Netlify（免费，自动部署）

1. 在 vercel.com / netlify.com 用 GitHub 登录
2. Import 仓库 → 框架选 `Other`（纯静态）→ Deploy
3. 得到 `https://xxx.vercel.app` 或 `https://xxx.netlify.app` 地址
4. 以后 `git push` 自动触发重新部署

---

## 路线 B · 加后端（多人共享录入）

需要把「上传论文」和「导入总结」从 localStorage 改为读写云端 API。

### B1 · Supabase（免费层，最快）

- 建 Supabase 项目，建一张 `papers` 表（字段与 data/papers.js 一致）
- 网站 app.js 改为：读数据先查 Supabase，写数据（上传/导入）调 Supabase API
- 我作为维护者：更新内置论文仍走 data/papers.js 或直接写库

### B2 · Cloudflare Workers + KV（免费层，轻量）

- 用 Workers 写一个 REST API（GET/POST /papers），KV 存储数据
- 网站通过 API 读写；部署后全组共享同一份数据

### B3 · 轻量服务器（VPS）

- 任选一台 Linux 服务器，跑 Node/Express + SQLite（或直接 JSON 文件）
- 网站文件由 Nginx 托管，API 由 Node 提供

---

## 我（agent）作为更新维护者的工作方式

部署完成后，每次你说「更新」，我执行：

1. 运行完整更新工作流（见 `更新工作流.md`）
2. 本地生成最新 `data/papers.js` + `summaries/` + `papers/`
3. **发布**：
   - GitHub Pages / Vercel / Netlify：`git add . && git commit -m "更新: <日期>" && git push`（自动部署）
   - COS/OSS：上传更新的文件
   - 后端路线：把内置数据写入数据库
4. 回复你：新增 N 篇、台账总数，访问地址不变

> 需要我把更新流程里的「发布」步骤也自动化（例如 push 后自动部署），
> 可以在确定托管平台后把发布命令并入 `更新工作流.md`。

---

## 需要你确认的决策

1. **数据共享程度**：
   - 只读共享（成员浏览，我维护内容）→ 路线 A，今天就能部署
   - 多人协作录入（成员也能上传，全组共享）→ 路线 B
2. **托管平台**：有没有现成的 GitHub / Gitee 账号，或腾讯云 / 阿里云账号？
3. **域名**：需要自定义域名（如 ledger.xxx.cn）还是先用平台默认地址？

确认后我可以：把网站推送到仓库、配置自动部署、或按需加后端，并把发布步骤固化进「更新」工作流。

---

## ✅ 已完成的部署（2026-09-03）

- **仓库**: https://github.com/weltwww-dot/paper-ledger （public）
- **线上地址**: https://weltwww-dot.github.io/paper-ledger/
- **分支/源**: main 分支根目录，GitHub Pages 自动部署
- **发布方式**: 本地 `git commit + git push` → Pages 自动更新（约 1–3 分钟）
- 「更新」工作流已并入发布步骤（见 `更新工作流.md` §9.5）

> 备注：国内网络对 github.com / github.io 的访问可能不稳定，
> 若课题组访问困难，可后续把同一仓库镜像到 Gitee Pages 或腾讯云 COS（内容不变，多一个国内入口）。
