# CONTEXT.md · 领域词汇表（论文台账）

> 只收领域词汇与定义，不含实现细节。站点设计/工作流请见 README 与「更新工作流.md」。

## 论文条目（Paper Entry）
台账中一篇论文的完整记录，字段来自六段式总结：标题、作者、期刊/会议、年份、DOI、arXiv、
PDF 链接、研究方向、主题标签，以及一句话概括、问题与动机、方法、实验与结果、贡献与局限。
另有机器可读的「发表时间」与「内容状态」。

## 收录（Collect）
一篇论文被写入台账（上传/导入/更新抓取）的过程。收录顺序 = 存储顺序（先入库在前）；
页面「收录」区默认按**发表时间倒序**展示（同日并列按收录先后），
新收录论文若发表更早会排在对应时间位置，不保证置顶。

## 发表时间（Publication Date）
论文在线发表日期（YYYY-MM-DD），记录在每篇总结的「基本信息」里（`- **发表**: …`）。
它是「收录趋势」的时间轴：趋势按发表时间倒序取窗口，与录入先后无关。

## 内容状态（Content State）
每篇论文的内容完整度：`complete`（完整）／`partial`（部分）／`pending`（待补全）。
状态记在总结「基本信息」的 `- **内容状态**:` 行，随同步进入数据。

## 待补全（Pending）
公开渠道（OpenAlex / Semantic Scholar / Crossref / arXiv / 出版社页）都取不到摘要、
且暂无全文时的状态；卡片上带「待补全」徽标，进入 `run_update.py instsci` 的机构补全队列。
取回全文并更新总结后，状态改为「完整」。

## 机构访问路线（Access Route）
取封闭访问全文的两条路线：**出口直连**（本机出口 IP 在订阅机构范围内，出版社按 IP 放行，
可见浏览器过 WAF 人机验证即可）与**机构 SSO**（CARSI/Shibboleth 等统一认证，登录后授权与出口 IP 无关）。
普通代理会让出口 IP 离开订阅范围，破坏出口直连；出版社域名必须走直连（见 `proxy-rules/README.md`），
否则只能改用机构 SSO。

## 研究方向（Direction）
论文的大类归属，当前固定为「信息安全 / 人工智能」两大类；筛选、统计均按此分桶。

## 主题标签（Theme Tag）
人工为每篇论文标注的 1–3 个研究问题短语（如「大模型」「隐私保护」），用于跨方向观察
顶刊在研究什么。一篇论文可挂多个标签；与「研究方向」是两个维度。

## 研究热点（Hotspot）
在指定统计范围内（默认：近 90 天发表的论文 × 全部/信息安全/人工智能），
各主题标签的出现篇数排行——反映「当前这批顶刊论文扎堆在哪些主题」。
热点是截面快照，不带时间对比。

## 收录趋势（Trend）
统计范围 = 近 90 天发表的论文（滚动窗口，随当天推移）；在该范围内对比
「发表时间最新的 12 篇」与「窗口内其余论文」两个分组的主题占比变化：
占比上升记「上升」、此前没有而最新分组出现记「新进」、占比下降记「回落」。

## 更新（Update）
用户对「按上次更新基准抓取最新顶刊论文 → 补全内容 → 收录并发布」整套流程的简称，
完整步骤见「更新工作流.md」。
