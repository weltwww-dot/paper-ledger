# 论文台账 · 「更新」工作流

> 触发词：**更新**（例如「更新一下」「跑更新」「执行更新」）
> 目标：把从上次更新日期到现在的全部期刊论文收录进台账，绝不遗漏、绝不重复。

---

## 0 · 总览

```
读基准 → 双来源增量抓取(全部论文) → DOI 对账 → OA 检查 → 抓摘要 → 补查 → 元数据核验
      → 登记六段式总结草稿 → 执行 agent 对照原文写中文六段式 → 按既有主题方案补齐标签 → PDF 探测/下载/校验/证据登记
      → 同步进台账 → 中文摘要闸门 + 中文文案质量闸门 + 主题闸门 + PDF 闸门 + 台账总体验收 → 推进基准 → QA → 交付
```

每次更新的起点是 `skill-runs/last_update.json`（记录上次日期 + 已收录 DOI）。
抓取范围 = 上次更新日期 **到现在** 的每本期刊**全部**论文，并默认重查最近 7 天的发表记录（不是只取最新一篇），用来覆盖来源收录延迟。

### 0.1 · 标准完成条件

一轮更新只有同时满足以下条件才算完成：

1. 每篇新增论文都有一份 `summaries/*.md`，包含六个固定段落；中文内容必须由执行 agent 对照可核验原文撰写，不能把英文摘要或自动直译当作最终交付。
2. 每篇新增论文都执行过 PDF 探测；能获取的 PDF 必须复制到 `papers/` 顶层并通过 `%PDF` / `%%EOF` 校验，确认不可得或确认是非研究页面的记录必须写入 `skill-runs/pdf_attempts.json`。
3. `node scripts/sync-papers.js` 已运行，且 `data/papers.js` 与 `summaries/` 的 DOI、状态、主题和 PDF 链接一致。
4. 五道闸门全部通过：`summary_gate.py`、`summary_quality_gate.py`、`theme_gate.py`、`pdf_gate.py`、`workflow_gate.py`。
5. 只有五道闸门全绿后才允许执行 `advance`；只有用户明确要求发布后才执行 `publish`。

当前台账仍有一批历史技术债：已有 PDF 但 `contentState=partial` 的旧条目必须在后续全文补全时升级为 `complete`；总体验收会报告数量，后续更新不得继续增加该数量。

唯一交接入口是 `SESSION_HANDOFF_2026-09-07.md`：接手前先读它，完成一轮后只更新该文件的当前状态和更新日志，不另建临时 handoff。

---

## 1 · 读取更新基准

`skill-runs/last_update.json`：

```json
{ "date": "2026-08-30", "dois": ["10.1007/...", "..."] }
```

- `date`：上次更新日期（本次抓取从该日期起）
- `dois`：台账已收录的全部 DOI，用于去重

没有该文件时（首次运行），基准设为首次批量抓取日期，已收录 DOI 从 `data/papers.js` 提取。

---

## 2 · 完整日常更新（一键）

```bash
python scripts/run_update.py update
```

`update` 是日常触发「更新」时唯一推荐的入口；它把翻译和 PDF 获取纳入同一事务，任一步失败都会停止，不能随后推进基准或发布。它依次执行：

1. **双来源增量抓取与对账** `scripts/fetch_incremental.py --last-date <date> --out skill-runs/records_inc.json --audit skill-runs/collection_audit.json --existing-dois <dois>`
   - OpenAlex 与 Crossref 按各自 ISSN 并发查询 17 本期刊区间内的全部 `article`
   - 默认用 `--lookback-days 7` 重查近期发表记录；需要更长回溯时显式传参，避免把历史建库误当作日常更新
   - 自动过滤社论 / 公告 / 封面 / 勘误等非论文，并在审计中记录过滤原因
   - 按 DOI 跳过已收录条目（**重复绝不添加**）
   - 写入 `skill-runs/collection_audit.json`：逐刊记录两个来源的数量、排除原因、错误与仅单来源 DOI；Crossref 刚上线而 OpenAlex 尚未收录的论文会直接进入本轮。任一来源失败时不写新增记录，`advance` / `publish` 也会拒绝继续。
3. **OA 检查** `oa_check.py -i skill-runs/records_inc.json -o skill-runs/oa_inc.json --arxiv --concurrent`
   - 标记每篇的开放获取状态与可用 PDF 链接
4. **多渠道抓摘要** `scripts/fetch_content.py -i skill-runs/oa_inc.json -o skill-runs/content_inc.json
   --attempts skill-runs/content_attempts.json`
   - 渠道顺序：OpenAlex → Semantic Scholar → Crossref → arXiv 精确匹配 → 出版社落地页
   - 每篇结果带类型 `ok / absent / blocked / rate-limited / not-found / error`
     （被墙 ≠ 试过），attempts 缓存进 `skill-runs/content_attempts.json`
5. 把新增 DOI 登记为六段式 `summaries/*.md` 文件
6. **中文六段式审校**：执行 agent 对照取得的英文摘要或全文，以自己的话写成中文六段式；自动直译脚本已停用，不能把逐句机器译文直接发布
7. **主题标签补齐**：运行 `scripts/fill_theme_tags.py --write`，优先复用已有细分主题；只在无法准确归入细分主题时使用上位主题
8. **PDF 获取闭环**：探测 → 下载可靠候选 → `%PDF` / `%%EOF` 校验 → 写入确认不可得的跳过证据
9. 同步到 `data/papers.js`，执行中文摘要、中文文案质量、主题、PDF 和台账总体验收五道闸门

没有新增记录时，仍会执行两道闸门检查，避免既有数据的翻译或 PDF 记录被遗漏。

### 2.1 分步命令（仅排障或内容补全）

```bash
python scripts/run_update.py fetch       # 只抓取、OA 检查、抓摘要
node scripts/import_incremental.js       # 登记本轮记录
python scripts/run_update.py pdf         # 完整 PDF 获取：探测、下载、校验、证据登记
node scripts/sync-papers.js              # 同步数据
```

分步操作不改变规则：`advance` 和 `publish` 仍会检查中文摘要、中文文案质量、主题、PDF 与台账总体验收五道闸门。

### 2.2 登记本轮结果

抓取完成后运行 `node scripts/import_incremental.js`，把本轮 DOI 写入 `summaries/`：

- 已取得机器摘要的记录标为「部分」，仅作为执行 agent 撰写中文总结时的可核验素材；
- 受限或未取得摘要的记录标为「待补全」；
- 不填充未经抓取的作者、六段式结论或本地 PDF；
- 已存在 DOI 会跳过，脚本可重复执行。

然后完成中文总结、PDF 获取和 `node scripts/sync-papers.js` 更新 `data/papers.js`。这一步不会推进 `last_update.json`；中文总结、PDF 获取和质量检查完成前，更新基准仍保持不变。

> 脚本路径：`fetch_latest.py` / `oa_check.py` 在 skill 目录
> `D:/codex/.codex/skills/paper-summarize-fetch/scripts/`；`fetch_content.py` 在仓库 `scripts/`，
> 统一用 venv Python 执行。

---

## 3 · 补查摘要与元数据

- 摘要缺失时，用 Semantic Scholar 补查：
  `https://api.semanticscholar.org/graph/v1/paper/DOI:<doi>?fields=title,abstract,authors,year,venue`
- 作者 / 期刊 / 年份用 Crossref 批量核验：
  `https://api.crossref.org/works/<doi>`
- arXiv 论文必要时打开摘要页确认作者与版本
- 仍查不到摘要时，按「素材补全管道」逐层尝试（**目标是让每篇文章都有真实内容，不出现占位**）：
  1. **Crossref abstract**：`api.crossref.org/works/<doi>` 的 `abstract` 字段（JATS，去标签）
  2. **出版社文章页摘要**：Springer `link.springer.com/article/<doi>`、Nature `nature.com/articles/<尾号>`
     页面可抓（curl/urllib + 浏览器 UA），提取 `citation_abstract` / `description` meta
  3. **arXiv 标题精确匹配**：`export.arxiv.org/api/query?search_query=ti:"<完整标题>"`，
     必须核对返回标题与论文一致（首词宽查会命中无关论文，禁止直接采用）
  4. **Semantic Scholar 搜索**：`/graph/v1/paper/search?query=<标题>`（注意 429 限流）
  5. **ScienceDirect PII → pdfft**：DOI 解析出 `pii/XXXX` 后可构造 pdfft 直链尝试全文
- 以上渠道都拿不到内容（常见于 Elsevier/IEEE 付费墙）：写「基于标题与元数据的保守转述」
  ——只陈述标题/作者/期刊可证实的信息，字段不写「原文未提供」，改为「论文暂缺公开摘要，
  具体 X 需获取全文后详述」，并在交付中提示该篇需要全文（可走 instsci 或用户提供 PDF）
- **绝不编造摘要、数字或结论**

---

## 4 · 写六段式总结

每篇新增论文生成一个 Markdown 文件到 `summaries/`，命名 `<主题>_总结.md`，结构：

```markdown
## 基本信息
- **标题**: …（原文语言）
- **作者**: …（前 3 位 + et al.）
- **期刊 / 会议**: … 年份
- **发表**: 2026-09-01（在线发表日期，来自 OpenAlex / Crossref）
- **内容状态**: 完整 / 待补全 · 公开摘要缺失（需全文）
- **研究方向**: 信息安全 / 人工智能
- **DOI**: …
- **arXiv**: …（如有）
- **PDF**: [文件名](papers/文件名.pdf)（如有）
- **代码**: …（如有）

## 一句话概括
提出什么、解决什么问题（1–2 句）。

## 问题与动机
现有方法痛点与重要性（2–3 句）。

## 方法
核心思想 + 关键机制，讲清为什么有效（2–3 句）。

## 实验与结果
数据集、基线、关键数字——只写原文提供的；缺失不编数字（2–3 句）。

## 贡献与局限
约 2 条贡献，再写局限与开放问题（2–3 句）。

---
DOI: …
```

**规则：**
- 中文输出，技术术语保留英文
- 基本信息里带 `- **发表**: YYYY-MM-DD`，趋势统计按此日期排序（不是收录先后）
- 基本信息里带 `- **内容状态**:`——真实内容（摘要或全文）读到写「完整」；
  公开渠道全试过仍无摘要（OpenAlex / S2 / Crossref / arXiv / 出版社页为空或被拦）
  写「待补全 · 公开摘要缺失（需全文）」，并进入补全循环（见 9.3）
- 数字可溯源：只写原文摘要/全文明确给出的数字
- 不写「原文未提供」式占位；保守转述只陈述标题/元数据可证实信息
- 用自己的话改写，不逐句照抄

**方向归类**（两大类）：
- **信息安全**：后门攻击、对抗攻击、隐私保护、日志/入侵检测、供应链安全、加密、网络犯罪、电子考试安全等
- **人工智能**：模型方法、优化算法、表示学习、强化学习算法、综述类等偏方法/AI 本体的内容
- 难以判断时按论文主要贡献的应用域决定；安全应用（异常检测、鲁棒性防护）倾向信息安全

**主题标签**（决定网站「研究热点与趋势」统计）：
- 每篇新增论文在 `data/theme-tags.json` 按 DOI 配置 1–3 个研究主题（中文短语）
- 尽量沿用已有主题词（大模型 / 多模态学习 / 隐私保护 / 入侵检测与防御 / 对抗与鲁棒性 /
  后门与投毒 / 密码学 / 异常检测 / 强化学习 / 组合优化 / 科学计算AI …），
  词表可在 `data/theme-tags.json` 里看全量；确有新主题再新增，避免同义重复标签
- 日常 `update` 会调用 `scripts/fill_theme_tags.py --write` 自动补齐缺失项，并由
  `scripts/theme_gate.py --check` 保证没有无主题论文；自动规则优先复用细分主题，
  “机器学习方法 / 数据管理与检索 / 系统与网络安全”等上位主题只用于信息不足的兜底项
- 只把 DOI 写进映射文件即可，`sync-papers.js` 会合并进 `data/papers.js`

---

## 5 · 下载 PDF（能下的才下）

**PDF 获取是每轮更新的必走步骤，不允许跳过。** 每篇新增论文都要完成：探测 → 尝试下载 →
确认不可得才标记跳过。可靠渠道按序尝试：

1. **arXiv**（oa_check 的 arxiv_id，或按标题回退 arXiv API）
2. **出版社文件直链**：Springer `link.springer.com/content/pdf/<doi>.pdf`（通常可下）、
   Nature / Zenodo 等开放仓库
3. **DOI 重定向解析**后得到的文章页，挖 `pdfft` / `article-pdf` / `doi/pdf` 直链再试
4. 只有确认为反爬（HTTPError / HTML 垃圾）或付费墙（closed）才跳过，只保留 DOI/arXiv 链接
5. **跳过必须有记录**：确认不可得时运行
   `python scripts/pdf_gate.py --mark <doi> <reason>`（reason ∈ blocked / not-oa / no-file / non_research_document），
   写入 `skill-runs/pdf_attempts.json`。闸门（`advance` / `publish` 前置）会检查：
   无 PDF 且无跳过记录的论文会**阻止推进**，杜绝整轮跳过 PDF 的情况。

`run_update.py pdf` 现在是完整获取命令，不是只探测：它会运行智能探测、下载 `direct` / `arxiv` / `article-pdf` 候选、校验文件，并把确认的不可得情况写入 `pdf_attempts.json`：

```bash
python scripts/run_update.py pdf          # 最近一次 fetch：探测 → 下载 → 校验 → 证据登记
```

探测结果四类：
- `direct` / `arxiv` → 有文件直链，直接下载
- `article-pdf` → 从文章页挖出了 PDF 直链（含 ScienceDirect pdfft），尝试下载，被 WAF 拦则跳过
- `blocked` → 出版社反爬或 403，登记为 `blocked`（仅给 DOI 链接）
- `error` → 网络或服务暂态错误，**不**自动标记跳过；必须重试或完成可审计核实，PDF 闸门会阻止推进
- `not-oa` → 付费墙，跳过（仅给 DOI 链接）

下载命令（探测返回直链后）——**优先使用项目内下载器**（失败自动清理、检测到 HTML 立即删除）：

```bash
python scripts/smart_pdf.py --doi <doi> --url <pdf-url> --out papers/<期刊缩写>_<年份>_<短名>.pdf
python scripts/bulk_download.py     # 批量下载（JOBS 列表）
```

若使用 skill 的下载脚本：

```bash
powershell -ExecutionPolicy Bypass -File <skill>/scripts/download_pdf.ps1 \
  -Url <pdf-url> -OutFile papers/<期刊缩写>_<年份>_<短名>.pdf -TimeoutSec 240
```

- 校验：`%PDF` 文件头 + `%%EOF` 文件尾 + `pdfinfo` 页数
- 超时/断点：`-TimeoutSec` 调大后重试（脚本支持断点续传）
- 出版社反爬（Cloudflare / JS 挑战）返回 HTML：**快速失败不重试，并删除失败残留文件**
  （download_pdf.ps1 失败会留下 HTML 垃圾 .pdf，必须清理），只保留 DOI 链接
- 付费墙（closed）：只给 DOI 链接；如需全文走 instsci 机构浏览器（用户提供机构权限并手动 SSO）
- 命名约定：`papers/<期刊缩写>_<年份>_<短名>.pdf`（ASCII）
- 下载完成后运行 `python scripts/run_update.py verify` 兜底校验 papers/（防垃圾进仓库）

---

## 6 · 同步进台账（一键）

```bash
node scripts/sync-papers.js
```

- 扫描 `summaries/*.md`，按六段式模板解析
- 按 DOI 合并 `data/theme-tags.json` 的主题标签（未配置的会打印警告）
- 解析每篇的「内容状态」（完整 / 待补全）并写入数据
- 生成 `data/papers.js`（网站数据）
- **新增总结排在最前**（最新收录置顶）
- 已收录条目保持原 id 与相对顺序
- 按标题去重，重复的绝不重复添加

---

## 7 · 推进更新基准（一键）

```bash
python scripts/run_update.py advance
```

- 从 `data/papers.js` 提取全部 DOI
- 更新 `skill-runs/last_update.json`：`date = 今天`，`dois = 全部 DOI`
- 下次更新从今天起抓取，已收录的自动跳过
- **前置闸门**：自动运行 `python scripts/summary_gate.py --check`、`python scripts/theme_gate.py --check`、`python scripts/pdf_gate.py --check` 和 `python scripts/workflow_gate.py --check`。依次保证中文六段式结构、每篇至少一个主题、无 PDF 论文都有可追溯的跳过记录，以及 DOI/总结/状态/主题/PDF 链接彼此一致。任一失败都会拒绝推进。

PDF 状态查询：`python scripts/pdf_gate.py --list`（列出所有无 PDF 论文及其跳过原因）

---

## 8 · QA 验证

用 Playwright（bundled runtime）验证：

1. `data/papers.js` 篇数正确、**无重复标题**、全部有方向
2. 页面统计与筛选计数一致（全部 / 信息安全 / 人工智能）
3. 收录列表按「发表时间」倒序渲染，最新发表的论文在顶部、卡片显示具体日期
4. 「研究热点与趋势」正常渲染：只统计近 90 天发表的论文（主题计数 = 窗口内配置标签数、
   点击主题可筛台账、统计范围切换有效）
   - 趋势在近 90 天窗口内取发表最新的 12 篇 vs 窗口内其余，文案与排序一致
5. 「内容状态」：每篇都有有效状态；pending（待补全）论文在卡片上有「待补全」徽标，
   数量与 `python scripts/run_update.py abstracts` 的重试清单一致
6. 旧浏览器数据升级：无重复、用户自定义条目保留、用户删除的内置论文不恢复、新增论文补上
7. 320 / 375 / 414 / 768 px 无水平溢出、无控制台错误
8. `python scripts/run_update.py verify`：papers/ 下所有 PDF 均以 `%PDF` 开头、含 `%%EOF`，
   无下载失败的 HTML 残留（发现即删除）
9. `python scripts/summary_gate.py --check`、`python scripts/theme_gate.py --check`、
   `python scripts/pdf_gate.py --check` 与 `python scripts/workflow_gate.py --check` 均通过

---

## 9 · 交付

- 回复：新增几篇、台账总数、方向分布、PDF 情况（几篇有 PDF / 几篇只给链接）、
  **待补全篇数**（内容状态 = 待补全，需全文）
- 提示用户**刷新浏览器**查看

---

## 9.3 · 待补全论文的补全循环（读不到摘要时）

先自动重试多渠道摘要（结果带类型，attempts 记入 `skill-runs/content_attempts.json`）：

```bash
python scripts/run_update.py abstracts
```

渠道顺序：OpenAlex → Semantic Scholar → Crossref → arXiv 标题精确匹配 → 出版社落地页。
结果分 `ok / absent / blocked / rate-limited / not-found / error`——「被墙」不会伪装成「试过」。

仍取不到时生成机构全文补全队列（HITL，需要你完成一次机构登录）：

```bash
python scripts/run_update.py instsci
```

机构 IP 路线必须使用**本机网络出口**，不得使用普通代理 IP。输出的
`instsci_batch_local_ip.ps1` 会在启动 InstSci 前强制执行下面的出口检查；检测到环境代理、
Windows 系统代理/PAC 或缺少出版社直连规则时会拒绝启动：

```bash
python scripts/route_check.py --require-direct
```

判定代理会带走出版社流量时，按 `proxy-rules/README.md` 配置直连规则或临时关代理；
无法直连则用机构 SSO（CARSI）路线，不再依赖出口 IP。

通过检查后按输出执行 `instsci_batch_local_ip.ps1`，再用 InstSci 的可见 CloakBrowser
对单篇先行验证。它能保证不走普通代理，但不能凭出口检测推断机构订阅资格；出现 CAPTCHA、
SSO 或人机验证时由用户在可见浏览器中完成。成功后再批量取 PDF/页面；
取回后由 agent 更新对应总结，把该篇 md 的「内容状态」改为「完整」，重新 sync + 发布。

**CloakBrowser 下载超时修复**（broker 启动报 ConnectTimeout / “Primary download failed”）：
本机已有一份可用的 Chromium 146 缓存（`C:\Users\Administrator\.cloakbrowser\chromium-146.0.7680.177.5\chrome.exe`），
运行 instsci 前先设置（也可直接跑 `scripts/instsci_preflight.ps1`，会自动探测并设置）：

```powershell
$env:CLOAKBROWSER_CACHE_DIR = 'C:\Users\Administrator\.cloakbrowser'
```

设置后 instsci 不再联网下载浏览器；校园网 IP 授权 + 人机验证即可直接取全文，无需 SSO。

---

## 9.4 · InstSci 机构 PDF 获取踩坑记录（2026-09-07 实测）

> 本节沉淀真实踩坑与修复，供后续批量取 PDF 复用。**跑批量前先执行预检脚本**
> （自动清理残留进程并设置浏览器缓存）：
>
> ```powershell
> powershell -ExecutionPolicy Bypass -File scripts\instsci_preflight.ps1
> ```

| 症状 | 根因 / 修复 |
| --- | --- |
| `TargetClosedError: launch_persistent_context`，浏览器一启动就被关闭，整批 `broker_error` / `capture_failed` | **上一轮残留的 CloakBrowser 进程仍占用 `C:\Users\Administrator\.instsci\chrome-profile`**，新实例无法启动。跑前清理：`Get-CimInstance Win32_Process \| ? { $_.CommandLine -match '\.cloakbrowser' -and $_.CommandLine -match 'instsci\\chrome-profile' } \| % { Stop-Process -Id $_.ProcessId -Force }`。预检脚本自动做。 |
| `BrowserContext.new_page: Target page, context or browser has been closed`（长批量中途崩溃，非 WAF） | 大批量（50+ 篇）单浏览器上下文不稳定。**拆小批（单批 ≤ 25 篇）+ 断点续跑**（维护 remaining 清单，成功即从队列移除）；一批结束/崩溃后先跑预检清理再开下一批。 |
| ScienceDirect 返回「检测到异常活动 / automatically released」风控页 | **Elsevier 高并发（concurrency 2+）或批量过猛触发 SD WAF**。立即停止，等风控自动解除（几分钟到几十分钟）；重跑时 `--concurrency 1`，还不行则单 DOI 逐篇。不要并发双出版社同打 SD。 |
| IEEE stampPDF 偶发 `Response ended prematurely` | 瞬时网络错误，instsci 自带 3 次重试；仍失败会走浏览器流程补取，一般可成功。 |
| CloakBrowser 首次运行联网下载超时 | 本机已有缓存，先设 `CLOAKBROWSER_CACHE_DIR`（见 9.3 上方说明）；预检脚本自动探测并设置。 |
| PDF 下载了但 `verified_match=False`、text 很短 | 下载到的是**期刊非论文页**（编委会 Editorial Board / 会员申请表 / 刊头 Publication Info），不是研究论文。**不要当论文入库**；在 `pdf_attempts.json` 记真实原因即可。示例：AIJ 编委会页、INN/ENNS 会员表、IEEE TAI Publication Information。 |
| `papers/instsci/` 很大且输出层级重复（`complete/pdfs` 与 `oa_first/pdfs` 常同文件） | 该目录是**中间产物**，已在 `.gitignore`；规范 PDF 只复制到 `papers/` 顶层（`papers/<前缀>_<年>_<slug>.pdf`）。数「已下 PDF」按唯一文件名/attempts 状态，不要直接 Recurse 计数（会翻倍）。 |
| 需要按 DOI 复核是否已下过 / 找 PDF | 看批次目录 `*\complete\manifest.csv` 与 `attempts.jsonl`（含 `verified_match`）；成果可统一归集到 `papers/instsci/verified_resume/`。 |
| 想跳过某出版社某批 | 维护 `skill-runs/instsci_*_remaining.txt`（每行一个 DOI），续跑直接喂给 instsci，已完成即从清单删除。 |
| 浏览器人机验证 / SSO / 2FA | InstSci 是 HITL：可见浏览器弹出时由**用户**完成验证；agent 不要绕过 CAPTCHA/WAF，也不要提交凭据/Cookie。 |

批量入口要点（Elsevier 触发风控时按上表降级）：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\instsci_preflight.ps1    # 清理+设缓存
instsci papers <dois.txt> --publisher elsevier --institution "中国农业大学" `
  --oa-first --login-timeout 900 --pdf-timeout 120 --concurrency 1 --no-broker
```

---

## 9.5 · 发布（部署到 GitHub Pages 后）

本地验证通过后，把更新发布到线上（仓库 `weltwww-dot/paper-ledger`）并**确认线上已更新**：

```bash
git add -A
git commit -m "更新: <YYYY-MM-DD> 新增 N 篇论文"
python scripts/run_update.py publish
```

`publish` 会先运行中文摘要闸门、PDF 文件校验和 PDF 闸门；只有全部通过，才会依次：

1. 检查并 **git push**（github.com 网络不稳定时自动重试 6 次）
2. 轮询 GitHub Pages 构建状态直到 **built**
3. 抓取线上 `data/papers.js`（带版本参数绕过 CDN 缓存），**对比线上与本地论文数**
4. 一致才输出「发布完成」；不一致则报错提示等待后重试

> GitHub Pages 是 push 后自动重建，通常 1–3 分钟生效，**不是即时的**；
> 浏览器查看时若仍显示旧内容，先硬刷新（Ctrl+F5）——数据文件已带版本号（`data/papers.js?v=…`），
> 每次同步自动更换，正常不会命中旧缓存。

> 若修改只涉及内容（summaries/papers/data），照常推送即可；
> 发布前确认 data/papers.js 已由 sync-papers.js 重新生成。

---

## 关键文件

| 文件 | 作用 |
| --- | --- |
| `scripts/run_update.py` | 日常完整更新（update）与推进/发布入口 |
| `scripts/summary_gate.py` | 中文六段式摘要闸门（结构与翻译完成性） |
| `scripts/fill_theme_tags.py` / `scripts/theme_gate.py` | 缺失主题补齐与全量主题闸门 |
| `scripts/workflow_gate.py` | 总体验收：DOI、总结、状态、主题、PDF 链接与跳过证据一致性 |
| `skill-runs/workflow_baseline.json` | 历史 `partial + PDF` 技术债上限，只允许下降，不允许新增 |
| `scripts/fetch_incremental.py` | 按日期区间抓取全部期刊论文（双来源 DOI 对账） |
| `scripts/journal_collection.py` | 统一期刊收录模块：OpenAlex / Crossref 适配、DOI 合并与逐刊审计 |
| `data/journals.json` | 17 本期刊的 OpenAlex / Crossref ISSN 目录 |
| `scripts/fetch_content.py` | 摘要多渠道收口（typed outcome + attempts 缓存） |
| `scripts/sync-papers.js` | 扫描总结 → 生成网站数据 |
| `shared/paper-parser.js` | 六段式解析器（浏览器与 sync 共用，修一处两处生效） |
| `shared/ledger-store.js` | 台账存储引擎（迁移/合并/删除记忆收口，接口 list/add/remove） |
| `scripts/dedup.py` | 「已收录」判定（DOI 或标题命中即重复，抓取与合并共用） |
| `skill-runs/last_update.json` | 更新基准（上次日期 + 已收录 DOI） |
| `summaries/` | 六段式总结（每篇一个 md） |
| `papers/` | PDF 文件 |
| `data/papers.js` | 网站数据（浏览器加载） |
| `data/theme-tags.json` | 主题标签映射（DOI → 研究主题，人工维护） |
| `shared/theme-stats.js` | 热点/趋势统计（浏览器与 QA 共用） |
| `index.html` / `app.js` / `styles.css` / `tokens.css` / `fonts.css` | 网站本体 |

---

## 边界情况

- **某期刊区间内无新文章**：跳过，不报错
- **某篇无摘要**：内容状态标「待补全 · 公开摘要缺失（需全文）」，仍收录；
  进入 9.3 补全循环（用户要求全部论文都加上，缺内容不假装已总结）
- **抓到的是刊头、编委会、投稿须知或会员表**：按非研究性页面处理，保留 DOI 和审计 note，
  在 `pdf_attempts.json` 使用 `non_research_document`，不复制到 `papers/`，不伪造研究论文总结
- **PDF 下载超时**：加大 `-TimeoutSec` 续传；大文件（>10MB）优先 arXiv
- **长时间未更新、新增很多**：逐篇处理，可分批交付但最终全部收录
- **用户已有浏览器数据**：自动升级合并，不丢用户数据；用户删除过的内置论文不复活
